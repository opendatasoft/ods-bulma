{
  "name": "ods-bulma",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dart-sass --watch --recursive --output export sass",
    "build": "dart-sass sass:export && npx postcss export/main.css > export/ods-bulma.min.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Opendatasoft",
  "license": "MIT",
  "dependencies": {
    "bulma": "0.9.1",
    "bulma-timeline": "^3.0.4",
    "cssnano": "^4.1.10",
    "dart-sass": "latest"
  },
  "devDependencies": {
    "postcss": "^7.0.35",
    "postcss-cli": "^7.1.2"
  }
}
