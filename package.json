{
  "name": "ods-bulma",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "node-sass --watch --recursive --output export sass",
    "build": "node-sass --recursive --output export sass && npx postcss export/main.css > export/ods-bulma.min.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Opendatasoft",
  "license": "MIT",
  "dependencies": {
    "bulma": "^0.9.1",
    "cssnano": "^4.1.10",
    "node-sass": "^5.0.0"
  },
  "devDependencies": {
    "postcss": "^7.0.35",
    "postcss-cli": "^7.1.2"
  }
}
